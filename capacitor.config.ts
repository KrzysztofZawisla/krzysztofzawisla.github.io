import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.ionic.krzysztofzawislaportfolio",
  appName: "Krzysztof Zawisła - Portfolio",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
