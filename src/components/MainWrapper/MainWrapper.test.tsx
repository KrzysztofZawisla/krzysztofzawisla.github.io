/*import MainWrapper, {
  MainWrapperType,
} from "@root/components/MainWrapper/MainWrapper";
import { render, cleanup, RenderResult } from "@testing-library/react";
import { MemoExoticComponent } from "react";
import { expectType } from "tsd";

afterEach(cleanup);

describe("MainWrapper", (): void => {
  it("renders component properly", (): void => {
    const { container }: RenderResult = render(<MainWrapper />);
    expect(container).toMatchSnapshot();
  });
  it("Has correct type", () => {
    expectType<MemoExoticComponent<MainWrapperType>>(MainWrapper);
  });
});
*/

export {};
